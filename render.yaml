services:
  - type: web
    name: hermes-agent-web
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm --filter web build
    startCommand: npx http-server hermes_cli/web_dist -p $PORT
    envVars:
      - key: NODE_ENV
        value: production
